language: php

cache:
  directories:
    - ~/.composer/cache

services:
    - redis-server

php:
  - 5.4

sudo: false

before_install:
  - phpenv config-add .travis.php.ini

install:
  - composer install --no-progress --prefer-dist

script: ./bin/phpunit

after_script:
  - CODECLIMATE_REPO_TOKEN=ba11992087a6a701b9ff4a8b009495ed075a1152087e546b534241bfea1f6644 ./bin/test-reporter
